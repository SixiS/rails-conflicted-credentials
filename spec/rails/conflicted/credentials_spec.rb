# frozen_string_literal: true

RSpec.describe Rails::Conflicted::Credentials do
  it "has a version number" do
    expect(Rails::Conflicted::Credentials::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
